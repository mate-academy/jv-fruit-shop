package core.basesyntax.FruitShopService;

import core.basesyntax.DB.Storage;
import core.basesyntax.model.FruitTransaction;

public class SupplyOperation implements Operation {
    @Override
    public void process(FruitTransaction fruitTransaction, Storage storage) {
//        String fruit = new Fruit(fruitTransaction.getFruit());
//        int leftover = storage.getFruitLeftover(fruit);
//        storage.setFruitLeftover(fruit, (leftover + fruitTransaction.getQuantity())));
    }
}
