package core.basesyntax.strategy.transaction;

public class SupplyTransactionHandler extends AddingTransactionHandler {
    @Override
    public int getAmount(int amount, int newAmount) {
        return super.getAmount(amount, newAmount);
    }
}
